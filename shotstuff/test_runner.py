# import sqlalchemy

# from shotstuff import test_session

# def runtests():
#     engine = sqlalchemy.create_engine('sqlite://')

#     # It's a scoped_session, and now is the time to configure it.
#     test_session.Session.configure(bind=engine)

#     run_the_tests